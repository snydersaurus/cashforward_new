'use strict';

/* Controllers */

angular.module('cashforward.controllers', [])
  .controller('OverviewController', ['$scope','$routeParams',function($scope, $routeParams) {
	
 }])
  .controller('BillsDepositsController', ['$scope','$routeParams',function($scope, $routeParams) {
	
 }])
  
